branch_protection:
  policies:
    - target: default_branch
      message: "Изменения основной ветки принимаются только через предложение изменений"
      rules:
        - prevent_force_push
        - prevent_non_pr_changes
        - prevent_deletion
