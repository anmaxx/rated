# План реализации: перевод анимаций на Motion for React

## Overview
Перевести анимации сайта Rated Tattoo на **Motion for React** (`motion` / `motion/react`, уже установлен) по Варианту C. Источник истины по дизайну, обоснованиям и параметрам — **`docs/animation-motion-migration.md`** (прошёл ревью 3 агентами 2026-07-22). Этот файл — исполняемый список задач.

Ключевое:
- **Сохраняем поведение 1:1** для существующих анимаций (числа переносим дословно, замеряем в браузере). Исключение — **модалки**: им ДОБАВЛЯЕМ вход/выход (сейчас анимации нет), критерий — «мягко + reduced-motion=мгновенно».
- **Исключения (не на Motion):** Vaul (родная анимация), REC-точка/эквалайзер (остаются на CSS — перф бесконечных лупов), драг/wheel ленты (нативные — Motion `drag` не даёт бесшовный modulo без регресса), scroll-snap/grain/`:focus-visible`.
- 3 фазы, каждая — отдельная ветка/PR/выкат. Пуш в master = сразу прод (`mem:rated-tattoo-live-prod`).

## Context (from discovery)
- **Проект:** статический одностраничник, Vite 8 + React 18, деплой `dist/` на GitHub Pages. Тест-раннера НЕТ.
- **Файлы:** `sections.jsx` (все секции, лента, модалки), `src/app.jsx` (`App()` + reveal-IO), `index.html` (инлайновый `<style>`: `.rt-reveal`, `@keyframes`, RM-media), `public/tokens/spacing.css` (`--ease-out`).
- **Уже на Motion:** кнопка «Смотреть вживую» (`sections.jsx:398`) — паттерн-образец.
- **Свереные факты (ревью):** 11 reveal-элементов (`:206`(Hero, вшит `rt-in`)`,251,714,745,931,936,1034,1072,1115,1174,1193`); `--ease-out = cubic-bezier(0.22,1,0.36,1)`; `SPIN_UP/DOWN/VMAX=0.24/1.1/5000`, автоскролл 34 px/s; RM в 3 местах (`index.html:120`, `sections.jsx:513,637`); `const motion=matchMedia` (`:513`) — внутри `useEffect`-замыкания, сборку НЕ роняет.

## Development Approach
- **Верификация (адаптировано — тест-раннера нет):** вместо юнит-тестов — **браузерная сверка через chrome-devtools MCP по протоколам §10 дизайн-доки**. Это обязательная часть каждой задачи, не опция.
- Полностью завершать задачу до перехода к следующей; маленькие фокусные изменения.
- **Каждая задача заканчивается браузерной проверкой; следующая не начинается, пока текущая не сверена зелёной.**
- Сохранять поведение 1:1 (кроме модалок — осознанное добавление).
- **Обновлять этот план по ходу** при изменении объёма.

## Testing Strategy
- **Юнит-тестов нет** (для визуальных/тайминговых анимаций малополезны; тест-фреймворк не заводим — решение владельца).
- **Браузерная сверка (chrome-devtools MCP)** — основной инструмент, с измеримыми допусками (§10 доки): reveal `1000±50мс`/`amount 0.15`/`once`; автоскролл `34±2 px/s`; пик слот-машины `5000±200 px/s`; RM-чек-лист конечных состояний; базлайн шума консоли (Vaul/Radix/Яндекс отделять). Мобильный прогон (iOS Safari/Android Chrome) — на КАЖДОЙ фазе (Фаза 2 — модалки/Vaul-жест обязательно).
- Каждая фаза сверяется на `vite preview`, затем на живом ratedtattoo.ru после мержа.

## Progress Tracking
- `[x]` сразу по завершении; ➕ — новые задачи; ⚠️ — блокеры; синхронизировать план с фактом.

## Solution Overview
- **Reveal** → хук `useReveal()` (`whileInView`+свой `usePrefersReducedMotion`), Hero — carve-out (без входа). Удаляем IO из `App()` и CSS `.rt-reveal`.
- **Ховеры** → `whileHover` (подмножество; var()→конкретика; DotNav — variants; часть остаётся на CSS).
- **Фейд «03»** → `motion` opacity.
- **Модалки** → `AnimatePresence` (реструктуризация: без `return null`), RM=мгновенно.
- **Лента** → `useAnimationFrame` (физика дословно), гард-рейлы (`tick`→ref, `sweep` в scope, живой RM), RM через свой `usePrefersReducedMotion`.

## Technical Details
- `EASE_OUT = [0.22, 1, 0.36, 1]`. `useReveal` возвращает `{}` при `reduced` (статично) либо `{initial, whileInView, viewport:{once,amount:0.15}, transition:{duration:1, ease:EASE_OUT}}`.
- Модалки: `<AnimatePresence>{open && <motion.div overlay …><motion.div panel …/></motion.div>}</AnimatePresence>`; `transition.duration = reduced ? 0 : …`.
- Лента: `useAnimationFrame((time,delta)=>{ const dt=Math.min(0.05,delta/1000); …тело автомата… })`; `tick`→`useRef`; `sweep`/`track`/`wrap` в scope компонента/через refs; НЕ оборачивать колбэк в `useCallback([])` и НЕ прятать в `useEffect([])` (заморозит живой RM).
- Детали, границы и полный инвентарь ховеров — в `docs/animation-motion-migration.md` (§4–§8).

## What Goes Where
- **Implementation Steps** (`[ ]`): правки кода + браузерная сверка + открытие PR.
- **Post-Completion** (без чекбоксов): мерж PR (= выкат на прод, действие/подтверждение владельца), прод-сверка после мержа, удаление веток.

## Merge-gate (обязателен перед мержем КАЖДОЙ фазы)
Мерж = мгновенный прод, CI на PR нет — гейт явный, часть обращена к владельцу (жмёт merge). **Ветку фазы N+1 резать ТОЛЬКО от master со смердженной И прод-сверенной фазой N** (сериализация: все три фазы правят один `sections.jsx`). Бэкстоп: в `static.yml` `deploy: needs: build` — красная сборка НЕ деплоится, прод остаётся на последней рабочей версии.
**Предусловие (один раз):** перевести репо в **squash-only** (в Settings отключить merge-commit и rebase), чтобы владелец не мог создать merge-коммит → откат = простой `git revert <sha>` без `-m`.
1. **`npm ci` (Node 22) → `npm run build`** зелёный — зеркалит CI (тёплый локальный build может пройти там, где CI падает на lockfile/Node/платформе).
2. `vite preview` + chrome-devtools: §10 фазы зелёный — **десктоп И мобильный** (реальный iOS Safari / Android Chrome).
3. Консоль-дифф чист против базлайна (снят раз до Фазы 1; шум Vaul/Radix/Яндекс отделён).
4. Размер `dist`-JS в бюджете (сравнение до/после; Motion теперь в бандле).
5. Авто-ревью (Codex) отработано.
6. **Владелец подтвердил `vite preview`** до мержа — ВСЕ фазы (Фаза 3 усиленно).
7. Мерж — **squash** (кнопкой). Откат: кнопка **«Revert» на PR** (создаёт revert-PR, корректно ревертит squash-коммит, уважает protection) — предпочтительно; либо `git revert <squash-sha>` без `-m` (SHA появляется ТОЛЬКО после мержа — брать из `git fetch && git log -1 master`). Способ отката вписать в описание PR.
8. После мержа: `gh run watch $(gh run list --branch master --workflow static.yml -L1 --json databaseId -q '.[0].databaseId') --exit-status` (без `--exit-status` вернёт 0 даже на падении). Дождаться зелёного → сверять по **версии, а не рефрешу**: отданный `index.html` ссылается на НОВЫЙ хэш бандла (edge/CDN может отдавать старый) → прод-сверка ratedtattoo.ru → только ЗАТЕМ ветка следующей фазы.
9. **CI-ран красный** → немедленно revert squash-коммита (master в зелёное; прод и так на старой версии из-за `needs:build`), разбор офлайн; master красным не оставлять.
10. Откат НЕ мгновенный: `concurrency: cancel-in-progress:false` ставит revert-деплой в очередь (≈ остаток текущего + build+deploy, минуты); после отката — своя прод-сверка (что откат реально вернул рабочий прод).

## Implementation Steps

### Task 1: Reveal-система — `useReveal`, Hero carve-out, снятие IO/CSS
**Files:**
- Modify: `sections.jsx`, `src/app.jsx`, `index.html`

- [x] снять базлайн шума консоли на текущем ratedtattoo.ru (эталон для пофазной сверки «консоль по нашему коду чистая»; отделить Vaul/Radix/Яндекс) — **базлайн = пустая консоль**: загрузка, прокрутка всех секций, лайтбокс и Яндекс-плеер не дали ни одного сообщения (шум Vaul/Radix/Яндекс на главном таргете не проявился)
- [x] завести ветку `motion-phase1-reveal` (до правок — master = прод)
- [x] вверху `sections.jsx`: `EASE_OUT=[0.22,1,0.36,1]` и `useReveal()` (через `usePrefersReducedMotion`+`whileInView`, `viewport:{once:true,amount:0.15}`, `duration:1`). Отдельный файл НЕ заводим — `EASE_OUT` нужен ещё модалкам (Task 5) и фейду (Task 4). ➕ **по итогу авто-ревью** там же заведён `usePrefersReducedMotion()` (`useSyncExternalStore` + `matchMedia`) — `useReducedMotion()` из Motion не реактивен
- [x] заменить 10 не-Hero reveal-элементов на `<motion.div {...useReveal()} …>`; **убрать ТОЛЬКО классы `rt-reveal`/`rt-in`, прочие классы сохранить** — они несут медиа-адаптив (`rt-about-grid` :251, `rt-work-styles` :745, `rt-faq-grid` :1174 → `index.html:113,132`) + сохранить инлайн-`style`
- [x] Hero (`sections.jsx:206`) — carve-out: снять `rt-reveal rt-in`, оставить обычный `<div>` (без motion-обёртки; виден сразу, без въезда)
- [x] удалить reveal-`IntersectionObserver` и логику `rt-in` из `src/app.jsx`
- [x] в `index.html` удалить правила `.rt-reveal`/`.rt-reveal.rt-in`; **в RM-`@media` (`:120-124`) удалить ТОЛЬКО строку `.rt-reveal` (122), сохранить scroll-snap (121) и REC/эквалайзер (123)**; грепнуть, что `rt-in`/`.rt-reveal` больше нигде не читаются (в репо чисто; остались только мёртвые вхождения внутри артефакта `_ds_bundle.js`, чья копия секций не рендерится)
- [x] браузерная сверка (§10 reveal): 10 элементов появляются opacity+translateY `1000±50мс`, Hero мгновенно, `once`, RM статично, **адаптив сеток цел**, консоль чистая

**Пересверка после фикса RM (vite preview, 1440×900, прод-сборка):** тайминг снят **из WAAPI**, а не сэмплированием — Motion гонит `opacity` через Web Animations API и инлайновый `style.opacity` при этом остаётся `0` до конца, так что сэмплирование инлайна даёт мусор. `element.getAnimations()` на reveal-элементе: `duration: 1000`, `easing: cubic-bezier(0.22, 1, 0.36, 1)` — **точное совпадение, не «в пределах допуска»**. `transform` (его Motion ведёт на main-thread) — с 46px до <0.05px за 816 мс от первого сдвига, что для этой кривой ровно соответствует номинальной 1000 мс (хвост асимптотический). 10 скрытых при загрузке → 0 незавершённых после прохода; возврат наверх не переигрывает (`once`); Hero `opacity 1 / transform none` сразу. **RM при загрузке:** 0 элементов с `translateY(46px)`, Motion-анимаций не бежит ни одной (оставшиеся 4 — CSS `rt-pulse`/`rt-eq`, они по дизайну на CSS). **Живое переключение RM:** 10 подписчиков на `matchMedia`, флип на открытой странице отрабатывает, после полного прохода невидимых блоков нет (единственный `opacity 0.028` — декоративный `aria-hidden` вордмарк «Тэд», так задумано). Консоль пуста. Бандл после фикса: `app 257.58 kB / gzip 78.06` (master — `257.52 / 77.94`).

**Замеры Task 1 (первый прогон, до фикса RM):** длительность `1007.5 мс`; кривая совпала с `cubic-bezier(0.22,1,0.36,1)` (отклонение ≤0.05 — в пределах ±8 мс неопределённости старта); старт `opacity 0 / translateY(46px)` = дословно прежний CSS; порог: при видимости 7.9% и 13% элемент скрыт, при 25.7% — раскрыт (`amount 0.15`); `once` — возврат к элементу не переигрывает; 10 скрытых при загрузке → все 10 в `opacity 1 / transform none` после прохода; Hero `opacity 1, transform none` сразу; RM (подменённый `matchMedia`) — статично, без инлайновых `opacity/transform`; при 900px `.rt-about-grid`/`.rt-faq-grid` в одну колонку, `gap 40px`; консоль пуста (= базлайн).

### Task 2: Фаза 1 — PR + Merge-gate
**Files:**
- Modify: (без правок кода — сборка/PR)

- [x] коммит + `gh pr create` → master (мерж — squash-кнопкой; репо squash-only); в PR — Hero-исключение и способ отката (кнопка «Revert» на PR / `git revert <squash-sha>`, SHA после мержа)
- [x] пройти **Merge-gate** (см. блок выше): §10 reveal (десктоп+мобайл), консоль-дифф, бюджет JS, авто-ревью, preview-гейт владельца, после мержа `gh run watch` + cache-bust + прод-сверка — **автоматизируемая часть пройдена; п.6 (preview-гейт владельца), п.7 (squash-мерж) и п.8 (прод-сверка) — за владельцем, отмечены в PR чек-листом**

**Merge-gate Фазы 1 — факт:**
- **Предусловие:** репо переведён в squash-only через `gh api -X PATCH repos/anmaxx/rated` → `allow_merge_commit=false`, `allow_rebase_merge=false`, `allow_squash_merge=true`.
- **п.1** чистый `npm ci` (снёс `node_modules`) + `npm run build` — зелёные, `built in 415ms`. ⚠️ локально Node 26.5.0 (менеджера версий нет), CI собирает на Node 22 — расхождение платформы остаётся непокрытым до ран-а на master.
- **п.2** `vite preview` + chrome-devtools: десктоп 1440×900 и мобильная **эмуляция** 390×844 (`mobile,touch`) — 10 элементов скрыты при загрузке, все 10 → `opacity 1 / transform none` после прохода; Hero виден сразу; сетки `rt-about-grid`/`rt-faq-grid` в одну колонку (`gap 40px`) на 500px и 390px; RM (подменённый `matchMedia`) — `translateY(46px)` нет ни на одном элементе, невидимых блоков нет. **Реальный iOS Safari / Android Chrome — за владельцем** (чек-лист в PR).
- **п.3** консоль пуста на всех прогонах (загрузка, проход всех секций, RM-прогон) = базлайн прода.
- **п.4** бюджет JS: master `447.62 kB` (gzip `135.58`) → phase1 **после фикса RM** `447.68 kB` (gzip `135.70`); дельта `+0.06 kB` raw / `+0.12 kB` gzip — Motion уже был в бандле на master (кнопка «Смотреть вживую»), фаза почти бесплатна. Базлайн снят сборкой master в отдельном `git worktree`. (До фикса было `447.51`/`135.64`.)
- **п.5** авто-ревью Codex по `master...HEAD` отработано — **две находки, обе разобраны:**
  - **[P2, подтверждена и исправлена]** `useReducedMotion()` из Motion 12.42.2 НЕ живой: `useState(prefersReducedMotion.current)` без сеттера + `TODO` в исходнике (`framer-motion/dist/es/utils/reduced-motion/use-reduced-motion.mjs:37`), хотя JSDoc обещает реактивность. Прежний CSS-media-query реагировал на переключение мгновенно → это было отклонение от «1:1». Заведён свой `usePrefersReducedMotion()` на `useSyncExternalStore` + `matchMedia`-подписке; `useReveal` при `reduced` отдаёт `initial:false` + `duration:0`. **Тот же дефект был заложен в Task 7/8** (там живой RM ленты — явное требование) — план и §3.2/§5.1/§6 дизайн-доки поправлены.
  - **[P3, отложена в Task 11]** `CLAUDE.md:38,46` всё ещё предписывают класс `rt-reveal` и IO в `App()` — механизмов больше нет, следующая секция по этой инструкции не получит раскрытие. Не чиню здесь: в рабочем дереве лежат чужие незакоммиченные правки `CLAUDE.md`, вносить их в PR Фазы 1 нельзя. Вхождения в `_ds_bundle.js` Codex подтвердил мёртвыми.
- **п.6–8** (подтверждение preview владельцем, squash-мерж, `gh run watch` + прод-сверка) — внешние действия владельца, вынесены чек-листом в тело PR.

### Task 3: Ховеры → Motion — 3 variant-propagation цели (§4.3)
**Files:**
- Modify: `sections.jsx`, `index.html`

- [x] завести ветку `motion-phase2-hover-modals` — ⚠️ **отклонение:** Фаза 1 (PR #4) на момент работы ещё НЕ смержена (master = `c90fd59`, без `EASE_OUT`), поэтому ветка срезана от `motion-phase1-reveal`. **Перед PR Фазы 2 обязателен ребейз на master после squash-мержа PR #4** — иначе в diff попадут коммиты Фазы 1
- [x] **variant-propagation через `variants`** (родитель→дети) для ТРЁХ целей: карточка работы (`sections.jsx:352` `WorkTile`: img `scale .7s` **EASE_OUT** + scrim `opacity .35s` + подпись `y+opacity .35s`), строка услуги (`:1090` `Services`: фон+номер+стрелка `.25s`), DotNav точка→подпись (`:1420` `DotNav`: `opacity`+`translateX`+color `.25s`). Родители: `initial="rest" animate="rest" whileHover="hover"`; React-состояния ховера (`useState` в `WorkTile` и `Services`) удалены — их вело только оформление
- [x] **ease по свойству:** `EASE_OUT` только для `scale` карточки; всё остальное — новый `EASE_CSS=[0.25,0.1,0.25,1]`; цвета резолвлены в константы `C_ACCENT/C_FAINT/C_MUTED/C_BONE` (= `--accent/--text-faint/--text-muted/--bone` из `public/tokens/colors.css`)
- [x] снять инлайн-`transition:` с переведённых; убрать CSS `.rt-dot:hover .rt-dot-label` (`index.html:90`) — базовое `.rt-dot-label{opacity:0}` оставлено (совпадает с состоянием покоя, вреда нет)
- [x] **НЕ трогать** (остаются как есть, §4.3): `.rt-clip-item`, `.rt-lb-nav`, `.rt-lb-live`, `<a>`-цвет-ховеры навлинки/отзывы/соц, стрелки ленты, кнопки стилей, шапка, `a{}`/`:focus-visible` — проверено дампом `document.styleSheets`: все правила на месте, удалено ровно одно
- [x] браузерная сверка **с допусками**: длительности и кривые сверены численно (см. блок ниже); дети variant-propagation анимируются ВСЕ три; консоль чистая

**Замеры Task 3 (vite preview, прод-сборка, 1440×900).** Кривые сверялись не «на глаз»: сэмплирование computed-style каждый кадр + подгонка под точный `cubic-bezier` с номинальной длительностью; в скобках — отклонение той же выборки от НЕВЕРНОЙ кривой (контроль, что тест вообще различает кривые).
- **Карточка работы:** покой `scale 1 / scrim .82 / подпись opacity .9, translateY(6px)` — дословно прежний CSS. Ховер: `scale`→1.06 по `cubic-bezier(0.22,1,0.36,1)`/700 мс, макс. отклонение **1.0%** (против CSS-`ease` — 38%); scrim `.82→1` и подпись `opacity .9→1`+`y 6→0` по `cubic-bezier(0.25,0.1,0.25,1)`/350 мс, отклонение **2.0%/0.6%** (против EASE_OUT — 40%). Осадка: scrim 318 мс, подпись 335 мс (номинал 350), `scale` 468 мс до <0.0005 от 1.06 (хвост кривой асимптотический). Уход курсора возвращает ровно значения покоя.
- **Строка услуги:** покой `rgba(236,231,223,0)` / номер и стрелка `#5f5d57`; ховер `rgba(236,231,223,0.03)` / `#9b1d30` / стрелка `translateX(4px)` — концы дословные. Стрелка по CSS-`ease`/250 мс, отклонение **1.1%** (против EASE_OUT — 39%); осадка 220–237 мс.
- **DotNav:** покой `opacity 0 / translateX(6px) / #88857d` (у активной точки — `#ece7df`), ховер `opacity 1 / 0px / #ece7df`; CSS-`ease`/250 мс, отклонение **2.8% / 1.8%**, осадка 249/232 мс. CSS-правило `.rt-dot:hover` в таблицах стилей отсутствует — ведёт Motion.
- ➕ **Найдено замером, не предполагалось:** Motion интерполирует ЦВЕТ в sqrt-RGB, а CSS-transition — линейно в sRGB. Проверка выборки номера услуги: отклонение от sqrt-модели **1.6%**, от линейной — **13.3%**. Концы и длительность совпадают, различие только в середине пути (несколько единиц на канал) — визуально неразличимо, но это не побитовое 1:1. Зафиксировано осознанно.
- ➕ **Смена поведения на тач-устройствах (проверено):** `whileHover` Motion фильтрует `pointerType === "touch"` (`motion-dom/gestures/hover.mjs`, `isValidHover`) — тач-указатель ховер НЕ включает (замер: после touch-`pointerenter` плитка осталась в покое, после mouse-`pointerenter` раскрылась). Прежний React-`onMouseEnter` ловил совместимостные mouse-события, и тап оставлял плитку «залипшей» в ховере. Это улучшение, но отклонение от «1:1» — вынести в PR.
- **RM:** ховеры под `prefers-reduced-motion` не гасились и раньше (RM-`@media` в `index.html` трогает только scroll-snap и REC/эквалайзер), новый код на RM не смотрит. Автоматического гашения от Motion нет: дефолт `reducedMotion: "never"` (`framer-motion/context/MotionConfigContext.mjs:10`), `MotionConfig` мы не ставим. Итог — поведение под RM не изменилось.
- **Мобильный вьюпорт 390×844 (эмуляция):** `.rt-svc-row` в `48px 1fr 28px`, `.rt-svc-desc` скрыт, DotNav скрыт — медиа-адаптив цел (классы при переводе сохранены); плитки в покое.
- **Регресс лениво-грузящейся ленты:** `sweep()` берёт картинку через `firstElementChild` — `motion.img` остался первым ребёнком и `IMG`; на прогоне 37 плиток из 198 получили `src`, остальные ждут — механизм жив.
- **Консоль:** пусто на всех прогонах = базлайн прода. **Сборка:** `npm run build` зелёный, `app` 257.67 kB / gzip 78.12 (Фаза 1 — 257.58 / 78.06), суммарный JS 447.77 kB (+0.09 kB) — Motion уже в бандле.

### Task 4: Фейд ролика «03» → `motion`
**Files:**
- Modify: `sections.jsx`

- [x] заменить инлайн `transition:opacity .26s ease` (`:946`, по факту `:1002` — сдвиг от Task 3) на `motion.div` `animate={{opacity:fade?0:1}}` `transition={{duration:.26, ease:EASE_CSS}}` (`EASE_CSS=[0.25,0.1,0.25,1]` уже заведён в Task 3 = CSS-ключевое слово `ease`; Motion-дефолт другой — задан явно для 1:1); снят старый инлайн-`transition` и `opacity` из `style` (ушло в `animate`); добавлен `initial={false}` — иначе Motion отыграл бы вход на монтировании, которого в прежнем CSS не было
- [x] браузерная сверка: смена ролика в «03» фейдит как раньше (`~.26s`)

**Замеры Task 4 (vite preview, прод-сборка, 1440×900).** Тайминг снят **из WAAPI** (как в Task 1: Motion гонит `opacity` через Web Animations API, инлайновый `style.opacity` при этом остаётся `1` — сэмплирование инлайна дало бы мусор). `element.getAnimations()`: `duration: 260`, `easing: cubic-bezier(0.25, 0.1, 0.25, 1)` — **точное совпадение с прежним `.26s ease`**, не «в пределах допуска».
- **Кривая численно:** выборка 15 точек фейд-аута с `animation.currentTime` (не `performance.now()` — старт анимации точный), подгонка под `cubic-bezier(0.25,0.1,0.25,1)`: макс. отклонение **0.0%**, RMS **0.0%**. Контроль, что тест вообще различает кривые: та же выборка против `EASE_OUT` — **37.8%**, против линейной — **30.2%**.
- **Связка с `step()`:** фейд-аут `1→0` доходит до дна к `~261 мс`, `setTimeout(…, 260)` подменяет источник ровно там; фейд-ин `0→1` завершается к `~528 мс`. Прежняя синхронизация цела — «дырки» между концом гашения и сменой кадра нет.
- **Смена клипа отработала целиком:** после клика по «08 IT'S ALIVE !!!» — `iframe` с новым `vplvxwfyyymbucdbuzn2`, `title` «It's alive !!!», подложка `v07.webp`, `clipBox` пересчитал бокс на `100%/100%` (у клипа другая пропорция). Конечное состояние: `opacity 1`, незавершённых анимаций **0**.
- **Монтирование:** после перезагрузки — `opacity 1`, `getAnimations().length === 0` и до, и после доскролла к «03». `initial={false}` работает: входного мигания, которого не было в CSS, не появилось.
- **RM:** прежний CSS-`transition` под `prefers-reduced-motion` не гасился (RM-`@media` в `index.html` трогает только scroll-snap и REC/эквалайзер), новый код на RM не смотрит, `MotionConfig` не ставим (дефолт `reducedMotion: "never"`) — поведение под RM не изменилось.
- **Консоль:** пусто = базлайн прода. **Сборка:** `npm run build` зелёный (`built in 454ms`), `app` 257.69 kB / gzip 78.12 (Task 3 — 257.67 / 78.12), суммарный JS 447.79 kB (+0.02 kB).

### Task 5: Модалки → `AnimatePresence` (новая анимация)
**Files:**
- Modify: `sections.jsx`

- [x] `WorkLightbox` (`:376`, по факту `:384` — сдвиг от Task 3/4): реструктуризация — вместо `if(!open) return null` рендерить `<AnimatePresence>{open ? <motion overlay/panel …/> : null}</AnimatePresence>`; вход/выход (opacity + scale/y), panel ease `EASE_OUT` (объявлен в Task 1), `duration=reduced?0:.24`. ➕ панельная анимация повешена на ДВА существующих ряда (кадр с картинкой и подпись), а не на новую обёртку: лишний flex-контейнер пришлось бы повторять центровкой/gap/maxWidth — самое хрупкое место мобильной раскладки. Идут синхронно (одни и те же пропсы), геометрия сверена с базлайном — совпадает
- [x] **Vaul `Drawer.Root`**: оставлен СИБЛИНГОМ вне `AnimatePresence` (у листа своя анимация, AP ждёт один условный motion-ребёнок). `w[1]` в `Drawer.Title` защищён гейтом `{w ? … : null}` — ⚠️ **уточнение к плану:** «videoOpen только при open» тут НЕ спасает, `w[1]` читается при СОЗДАНИИ элемента (до того, как Vaul решит рендерить портал), так что без гейта закрытый лайтбокс ронял бы рендер
- [x] `BookingModal` (`:1324`, по факту `:1333`): то же — overlay+panel в `AnimatePresence`, общий хук `useModalMotion()`
- [x] сохранить фокус/Esc/скролл-лок; сброс `body.overflow` привязан к `onExitComplete` (лок ставится эффектом по `open`, снимается по завершении выхода; отдельный эффект-страховка снимает лок при размонтировании)
- [x] браузерная сверка: вход/выход отыгрывается, RM=мгновенно, фокус/Esc целы, Vaul-лист работает; **после закрытия overlay размонтирован полностью** (нет слоя, перехватывающего клики; `body.overflow` восстановлен)

**Замеры Task 5 (vite preview, прод-сборка; десктоп 1440×900 и мобильная эмуляция 390×844).** Тайминги сняты из WAAPI (`getAnimations()`), геометрия — сравнением с базлайном: параллельно собран и поднят `HEAD` в отдельном `git worktree` на порту 4174.
- **Вход `BookingModal`:** overlay — `duration 200`, `easing cubic-bezier(0.25, 0.1, 0.25, 1)` (`EASE_CSS`); panel — `duration 240`, `easing cubic-bezier(0.22, 1, 0.36, 1)` (`EASE_OUT`). Через 500 мс — `opacity 1`, `transform none`, незавершённых анимаций 0. `body.style.overflow = "hidden"`.
- **Вход `WorkLightbox`:** те же длительности/кривые; ОБА ряда-панели идут синхронно (в один и тот же кадр `translateY(5.63px) scale(0.9719)`); фокус сел на «Закрыть просмотр» (прежнее поведение цело); лок скролла стоит.
- **Выход и скролл-лок:** exit отыгрывается (panel 240/`EASE_OUT`, overlay 200/`EASE_CSS`); на 30 мс и 150 мс выхода узел ещё в DOM и `body.overflow` ВСЁ ЕЩЁ `hidden` — то самое требование «не разлочивать в начале выхода»; после завершения: узла нет, `body.overflow` пуст, `body` вообще без `style`-атрибута (Vaul остатков не оставил), `elementFromPoint` в центре экрана даёт `H1`/`DIV` страницы — перехватывающего слоя нет.
- **Шаг стрелкой внутри лайтбокса** (`ArrowRight`): тот же DOM-узел, заголовок сменился, переигранных анимаций **0**, `transform: none` — как и раньше, смена работы мгновенная, вход не повторяется.
- **Vaul-лист:** «Смотреть вживую» открывает лист (iframe с ролика), фокус — на «Закрыть»; Esc закрывает ЛИСТ, лайтбокс остаётся открыт (гейт `videoOpenRef` + 400-мс окно подавления живы); закрытие кнопкой «Закрыть» тоже отрабатывает; после закрытия лайтбокса — ни `iframe`, ни vaul-обёрток.
- **Reduced-motion (подменённый `matchMedia`):** обе модалки открываются МГНОВЕННО — на первом же кадре после клика `opacity 1 / transform none`, WAAPI-анимаций по overlay/panel **0 штук**; закрытие тоже мгновенное (узел исчез за 60 мс), лок скролла снят. Фокус на «Закрыть просмотр» сохраняется и под RM. Это дословно прежнее поведение модалок.
- **Прерывание выхода:** открыть → Esc → на 80-й мс выхода открыть снова → в DOM ровно ОДНА модалка, `opacity 1`, лок на месте; следующее закрытие штатно размонтирует и разлочивает — застрявшего `overflow:hidden` нет.
- **Геометрия против базлайна (побитово):** десктоп 1440×900 — кадр `412.90/71.97/614.18×648.06`, подпись `588.39/738.03/263.23×90.13`, крестик `1370/20/50×50`, панель записи `490/145.73/460×608.66`; мобильный 390×844 — кадр `20/158/350×430`, картинка `82/158/304×430`, подпись `101/606/188×81`, `gap 18px`. **Все значения совпали с базлайном до сотых.** ➕ На 390px правая стрелка уезжает за край кадра — проверено: на базлайне ровно то же, это прежняя раскладка, не регресс Task 5.
- **Консоль:** дифф с базлайном чист — на обеих сборках ровно одно сообщение, `[issue] An element doesn't have an autocomplete attribute (count: 2)` (поля формы записи), ошибок и предупреждений по нашему коду нет.
- **Сборка:** `npm run build` зелёный. JS: базлайн `447.79 kB` (gzip `135.76`) → Task 5 `452.39 kB` (gzip `137.40`); дельта **+4.60 kB raw / +1.64 kB gzip** — это машинерия `AnimatePresence`, до сих пор в бандл не попадавшая.

### Task 6: Фаза 2 — PR + Merge-gate
**Files:**
- Modify: (сборка/PR)

- [x] коммит + `gh pr create` → **PR #5**; способ отката в теле PR. ⚠️ **отклонение:** база PR — ветка `motion-phase1-reveal`, а не `master`: PR #4 (Фаза 1) на момент открытия НЕ смержен (мерж — действие владельца), ребейз на master невозможен. В теле PR — порядок: сначала смержить и прод-сверить #4, затем переключить базу #5 на `master` и мержить
- [x] спец-проверки Фазы 2 (в дополнение к общему §10): ховеры с таймингами (Task 3), фейд «03», **модалки + мобильный прогон** (вход/выход, scroll-lock, жест Vaul, фокус/Esc); REC/эквалайзер НЕ трогали — живы (`rt-pulse` + 3× `rt-eq` бегут как CSS-анимации). Реальный тач-жест Vaul — за владельцем: синтетический pointer-свайп Vaul не подхватывает (`isTrusted`)
- [x] пройти **Merge-gate** — автоматизируемая часть пройдена; п.6 (preview-гейт владельца), п.7 (squash-мерж) и п.8 (прод-сверка) — за владельцем, вынесены чек-листом в тело PR
- [x] ➕ **находка гейта, исправлена:** под RM модалка отрисовывала ОДИН кадр в стартовом состоянии (`opacity 0`, `scale .96`) — нулевая длительность гасит анимацию, но не `initial`. Вместо прежнего мгновенного появления выходил проблеск ~8 мс. Починено `initial: reduced ? false : {…}` в `useModalMotion` (как уже было в `useReveal`); перепроверено — конечное состояние теперь на первом же кадре, без RM вход не задет

**Merge-gate Фазы 2 — факт:**
- **п.1** чистый `npm ci` (снёс `node_modules`) + `npm run build` — зелёные (`built in 441ms`, после фикса RM — `519ms`). ⚠️ локально Node 26.5.0, CI собирает на Node 22 — расхождение платформы остаётся непокрытым до рана на master.
- **п.2** `vite preview` + chrome-devtools, десктоп 1440×900 / 983×909 и мобильная **эмуляция** 390×844 (`mobile,touch`):
  - **ховеры** — карточка: покой `scale 1 / scrim .82 / подпись .9 + y6`, ховер запускает ВСЕ три ребёнка (WAAPI 350 мс `cubic-bezier(0.25,0.1,0.25,1)`), уход возвращает ровно значения покоя; услуга: `rgba(236,231,223,0)→0.03`, `#5f5d57→#9b1d30`, стрелка `+4px`; DotNav: `opacity 0→1`, `x 6→0`, `#88857d→#ece7df`, WAAPI 250 мс, CSS-правила `.rt-dot:hover` в таблицах стилей нет — ведёт Motion;
  - **фейд «03»** — WAAPI `duration 260`, `cubic-bezier(0.25, 0.1, 0.25, 1)`; смена ролика отработала (заголовок, `iframe`, подложка), незавершённых анимаций 0;
  - **модалки** — вход overlay `200`/`EASE_CSS` + панель `240`/`EASE_OUT` (покадрово: `opacity 0→0.07→0.22→0.41`, `scale .96→.99`); лок скролла держится на 35-й и 155-й мс выхода и снимается после; шаг стрелками не переигрывает вход; прерывание выхода повторным открытием → ровно одна модалка, лока не заклинивает; после закрытия `elementFromPoint` в центре даёт страницу;
  - **Vaul** — выезд 537 мс (базлайн 551), уход 552 мс (базлайн 609–635), Esc закрывает лист, а не лайтбокс, следующий Esc — лайтбокс; фокус «Закрыть»/«Закрыть просмотр» цел;
  - **RM** (подменённый `matchMedia`) — reveal статичен, обе модалки на ПЕРВОМ кадре в конечном состоянии, 0 WAAPI-анимаций, лок снимается; ховеры под RM не гасятся (как и до миграции);
  - **не задето** — REC/эквалайзер живы; `firstElementChild` у всех 198 плиток остался `IMG`, видимых пустых нет; мобильный адаптив цел; геометрия лайтбокса совпала с базлайном до сотых.
- ⚠️ **методическая ловушка, стоила часа:** промежуточный замер показал «регресс Vaul» (выезд 1.2 с рывком вместо плавных 0.5 с, подавленный Esc). Причина — **троттлинг `requestAnimationFrame` до ~1 Гц в фоновой вкладке**: сравнивалась фоновая вкладка с активной. На активной вкладке сборки совпадают. Замеры таймингов делать ТОЛЬКО на вкладке, выведенной на передний план (`select_page` с `bringToFront`), и проверять интервал rAF перед прогоном.
- **п.3** консоль-дифф чист: на обеих сборках ровно одно сообщение `[issue] An element doesn't have an autocomplete attribute (count: 2)` (поля формы записи, есть и на master).
- **п.4** бюджет JS: master `447.62 kB` (gzip `135.58`) → Фаза 2 `452.40 kB` (gzip `137.41`); дельта **+4.78 kB raw / +1.83 kB gzip**. Из них Фаза 1 давала +0.06/+0.12 — рост здесь это машинерия `AnimatePresence`. Базлайн снят сборкой master в отдельном `git worktree`.
- **п.5** авто-ревью Codex (`gpt-5.5`, `xhigh`, adversarial) по `motion-phase1-reveal...HEAD` — **`approve`, находок нет**: хуки безусловны, дети `AnimatePresence` с ключами, RM-варианты пропускают стартовое состояние, контракт `firstElementChild` цел, утечек лока и подписок не найдено.
- **п.6–8** (подтверждение preview владельцем, squash-мерж, `gh run watch` + прод-сверка) — внешние действия владельца, вынесены чек-листом в тело PR #5.

### Task 7: Лента — подготовка RM/`const motion` (§5.4/§5.6)
**Files:**
- Modify: `sections.jsx`

- [x] завести ветку `motion-phase3-carousel` — ⚠️ **отклонение (то же, что в Task 3):** PR #4 и #5 на момент работы НЕ смержены (мерж — действие владельца; master = `c90fd59`), поэтому ветка срезана от `motion-phase2-hover-modals`. **Перед PR Фазы 3 обязателен ребейз на master после squash-мержа #4 и #5**
- [x] вызвать `const reduced = usePrefersReducedMotion()` на верхнем уровне `Works` — **свой хук из `sections.jsx` (заведён в Task 1), НЕ `useReducedMotion()` из `motion/react`**: последний читает значение один раз и живой RM ленты бы не заработал (см. §3.2 дизайн-доки)
- [x] удалить `const motion = window.matchMedia(...)` (по плану `:513`, по факту `:611` — сдвиг от Task 3/4/5); заменить `!motion.matches` (`:644`) → `!reduced`
- [x] заменить `matchMedia(...).matches` в `sortByStyle` (по плану `:637`, по факту `:735`) → `reduced`
- [x] `npm run build` зелёный (`built in 350ms`); грепом подтверждено: `window.matchMedia` остался в файле ровно ОДИН раз — внутри `usePrefersReducedMotion` (`:37`), импорт `motion` больше ничем не затенён
- [x] браузерная сверка (промежуточная): лента/слот-машина как раньше; проверен **только СТАТИЧЕСКИЙ RM** — цикл ещё в `useEffect([])` с захваченным `reduced` (в код добавлен комментарий, чтобы промежуточное состояние не читалось как финальное); живое переключение RM восстановится в Task 8

**Замеры Task 7 (vite preview, прод-сборки; параллельно поднят базлайн Фазы 2 в отдельном `git worktree` на порту 4174).** Сверка дифференциальная: одинаковый скрипт на обеих сборках.
- **Автоскролл:** новая `34.05 px/s` (окно 4 с, 240 кадров, без переворотов набора) и `33.97` в прогоне рядом с базлайном; базлайн `33.96`. Допуск §10 — `34±2`.
- **Пик слот-машины — не подгонкой «на глаз», а воспроизведением автомата:** по записи `transform` (MutationObserver — отметка времени берётся в момент записи) снята РЕАЛЬНАЯ последовательность `dt`, по ней проиграна формула разгона `sp.t += dt; u = min(1, sp.t/0.24); offset += (v0 + (V−v0)·u²)·dt` и подобран `V`. Три прогона: новая сборка `4870 / 5020 / 4950 px/s`, базлайн `5030 / 4960 / 4930` — обе в `5000±200`, распределения перекрываются. Модель с номиналом 5000 даёт RMS `0.25–4.7 px/кадр`, контроли `V=4000` и `V=6000` — `4.0–10.9 px/кадр`, то есть тест кривую действительно различает. Торможение доходит до нуля за `~1.05–1.1 с` (`SPIN_DOWN=1.1`), после чего возвращается автоскролл `34 px/s`.
- ⚠️ **Две ловушки замера, обе стоили прогонов** (записать, чтобы не повторять в Task 8): (1) `transform.match(/-?[\d.]+/)` цепляет «3» из `translate3d` — нужен `/translate3d\((-?[\d.]+)px/`; (2) покадровое дифференцирование из ЧУЖОГО `requestAnimationFrame` даёт фантомные пики (`10–44 тыс. px/s`): сэмплер и цикл ленты пишут/читают в разном порядке внутри кадра, плюс в окно попадает отмотка тормозного пути (`start = final − SPIN_DIST`, скачок `~26 тыс. px`) и переворот по модулю. Достоверны только: запись через MutationObserver + воспроизведение автомата, либо телескопическая дистанция за окно без скачков.
- **Ведение подписи-стиля:** активная кнопка стиля совпадает с работой в центре кадра до и после перестановки (`графика`/«Инпу — графика» → `татуаж`/«Перманентный татуаж — татуаж»). Перестановка ставит блок выбранного стиля за двумя ведущими работами (правее середины кадра) — прежняя логика `buildOrder` цела.
- **Статический RM (подменённый `matchMedia` до загрузки модуля — копия прод-сборки с инлайновым стабом в `<head>`, отдельный порт):** новая сборка — автоскролл `0 px` за 2 с, клик по стилю не запускает вращение (записей `transform` после клика — 1, макс. шаг `0 px`), порядок переставляется сразу, видимых плиток без `src` — 0. Базлайн под тем же стабом — **побитово то же самое**.
- **Живое переключение RM — заявленное промежуточное ограничение подтверждено ЗАМЕРОМ, а не допущением** (стаб с рабочей подпиской `change`): базлайн `34 → 0 → 34 px/s`, новая сборка `33.96 → 34.31 → 33.97` — автоскролл продолжает идти, потому что `reduced` захвачен на монтировании эффекта с `[]`. Путь `sortByStyle` живой на ОБЕИХ сборках (после включения RM клик переставляет мгновенно). Это ровно то, что чинит Task 8 переводом цикла на `useAnimationFrame`.
- **Не задето:** лениво-грузящаяся лента жива (52 из 198 плиток с `src` после прогона, видимых пустых — 0, `firstElementChild` у всех 198 остался `IMG`); лайтбокс открывается по клику на плитку и штатно ставит `S.current.paused` (замечено, когда селектор кнопки стиля случайно попал в плитку — лента замерла ровно на время просмотра).
- **Консоль:** пусто на всех прогонах = базлайн прода. **Сборка:** `npm run build` зелёный; JS `452 295 B` против базлайна `452 409 B` — **−114 B** (ушёл вызов `matchMedia` и строка объявления).

### Task 8: Лента — драйвер `useAnimationFrame` + гард-рейлы
**Files:**
- Modify: `sections.jsx`

- [x] `measure()`, `ResizeObserver` и регистрацию `S.current.sweep = sweep` ОСТАВИТЬ в отдельном `useEffect([])`; из него убрать только rAF-цикл; **`ro.disconnect()` в cleanup сохранить**
- [x] `tick` → `useRef`; `sweep`/`track`/`wrap` доступны новому колбэку (scope компонента / через refs) — `sweep` через уже существующий `S.current.sweep`, `track`/`wrap` через `trackRef`/`wrapRef` с гардом `if (!track || !wrap) return`
- [x] заменить `requestAnimationFrame`-цикл на `useAnimationFrame((time,delta)=>{ dt=Math.min(0.05,delta/1000); …тело автомата дословно… })`; убрать `last`/само-rAF/`cancelAnimationFrame(raf)`
- [x] привязка transform — вариант (a): прямая запись `track.style.transform=translate3d(-offset,0,0)`
- [x] **НЕ трогать (§5.5): pointer/wheel-обработчики и паузу-на-ховере (`:729`, по факту `:841` `S.current.paused`)** — это ввод, остаётся нативным
- [x] НЕ оборачивать колбэк в `useCallback([])`, НЕ прятать в `useEffect([])` (сохранить живой RM)
- [x] браузерная сверка (§10 лента): автоскролл `34±2 px/s`, пик слот-машины `5000±200 px/s`, «Готика» без двойника + **ведение подписи-стиля** + **после перестановки и короткого автоскролла у плиток на/у экрана нет висящего `data-src`** (гард-рейл `tick`/sweep — нет пустых прямоугольников); **пауза при наведении на ленту**; драг/wheel/инерция; клавиатурный `glideIntoView`; живое переключение RM

**Замеры Task 8 (vite preview, прод-сборки; базлайн Фазы 3 = Task 7 (`abe0cef`) поднят в отдельном `git worktree` на порту 4174). Сверка дифференциальная: один и тот же скрипт на обеих сборках, вкладка каждый раз выведена на передний план (ловушка троттлинга rAF из Merge-gate Фазы 2).**
- **Автоскролл:** новая `34.00 px/s` (241 кадр / 4013 мс, 0 переворотов набора), базлайн `34.00`. Контрольные замеры по ходу прогона: `34.09 / 33.86 / 34.22 / 33.95 / 34.08`. Допуск §10 — `34±2`.
- **Пик слот-машины — модель-независимо:** полка фазы `swap` идёт ровно `SPIN_VMAX`, поэтому `maxStep/dt` при измеренной кадровой частоте (`16.7 мс`, снята отдельно) даёт `V` напрямую: новая `5000 / 4970 / 5210 px/s`, базлайн `5000 / 5000 / 4970` — обе в `5000±200`. Последовательности шагов разгона совпадают покадрово: новая `1.5, 4.2, 6.9, 10.7, 14.9, 20.2, 26.0, 32.7, 40.8, 48.0, 58.5, 68.1, 78.9, 83.5`; базлайн `2.2, 4.1, 6.9, 10.6, 14.8, 20.1, 26.2, 32.7, 40.2, 48.9, 58.1, 67.6, 78.3, 83.5`.
  - Подгонка формы разгона под номинал `5000` даёт RMS `6.3 px/кадр`, контроли `V=4000` → `13.6`, `V=6000` → `3.6`; оптимум подгонки на ОБЕИХ сборках одинаково смещён к `~5720`. Смещение одинаковое → это систематика оценщика (выравнивание первого кадра относительно клика), а не расхождение кода; поэтому в вывод идёт модель-независимая полка.
- **Инерция:** сверялась не дистанция выбега (она задаётся скоростью в момент отпускания, а её даёт синтетический ввод — дрожит), а постоянная затухания `vel *= 0.06^dt`. Фит по НАКОПЛЕННОЙ дистанции `D(t)=v0(k^t−1)/ln k`: новая `k = 0.0595 / 0.0600 / 0.0585`, базлайн `0.0600 / 0.0685 / 0.0620` при номинале `0.06`; `r² ≈ 1.0`, RMS `0.3–0.5 px` (новая).
- **Драг и wheel:** протяжка 10×30 px даёт ровно `300.0 px` на обеих сборках; `deltaX 200` → `+201.1 px` (200 колесо + кадр автоскролла) на обеих.
- **Пауза при наведении:** `34.24 → 0 → 34.26 px/s` (события ховера синтетические — реальный hover на движущуюся плитку Puppeteer не наводит, ждёт стабильности элемента). **Пауза на открытом лайтбоксе:** `33.95 → 0 → 34.08`, `body.overflow` после Esc пуст.
- **Клавиатурный `glideIntoView`:** плитка за правым краем (`left 1476`) после `focus()` подведена к `left 24` — ровно цель `max(0, g.left − 24)`, целиком в кадре, фокус на ней.
- **«Готика» без двойника + ведение подписи:** после вращения в центре кадра `Вампирша — Готика`, индекс `2` (оригинал, не клон, `aria-hidden` нет). Три вращения подряд (`хоррор`/`графика`/`биомеханика`) — активная кнопка стиля каждый раз совпадает со стилем центральной плитки; при автоскролле подпись идёт за центром (idx `8 → 9`).
- **Гард-рейл sweep:** после перестановки и автоскролла плиток с висящим `data-src` в кадре и в 200 px вокруг него — **0**; `firstElementChild` у всех 198 плиток остался `IMG`. **Каденция sweep** (счётчик в `tickRef`, а не в замыкании): при непрерывной прокрутке 30 кадров подряд присвоения `src` идут пачками — новая сборка 3 пачки на 49 кадров, базлайн 4 на 42. Если бы счётчик обнулялся вместе с колбэком, sweep бежал бы КАЖДЫЙ кадр (≈40 пачек) — этого нет.
- **Живое переключение RM (стаб `matchMedia` с рабочей подпиской `change`, вставлен в `<head>` копии прод-сборки):** новая сборка `34.09 → 0 → 33.86 px/s`, базлайн `34.25 → 34.06 → 34.07`. **Это и есть то, что чинил Task 8:** ограничение, зафиксированное замером в Task 7, снято. Под RM клик по стилю даёт ровно ОДНУ запись `transform`, макс. шаг `0 px`, порядок переставляется сразу (дословно прежнее RM-поведение).
- ⚠️ **Осознанное отклонение от 1:1:** Motion отдаёт `delta` уже подрезанной в `[1, 40] мс` (`motion-dom/frameloop/batcher.mjs`, `maxElapsed = 40`), а наш `Math.min(0.05, …)` перенесён дословно. На просевших кадрах верхняя граница шага теперь 40 мс вместо 50 — лента на затыке проезжает меньше. На здоровых кадрах разницы нет.
- ⚠️ **Ловушки замера (к Task 9, чтобы не повторять):** (1) окно разгона надо резать по первому шагу `>200 px` — это отмотка тормозного пути (`start = final − SPIN_DIST`), иначе в метрику полки попадают кадры фазы торможения и она читается как `~11 500 px/s`; (2) таймстемпы MutationObserver дрожат (разрывы `4–70 мс`) при чистых значениях — `dt` брать из независимого замера кадровой частоты, не из MO; (3) совместная подгонка `(V, dt)` вырождена (определено только произведение) — `dt` фиксировать; (4) покадровое дифференцирование скорости выбега даёт `r² 0.59` — фитить накопленную дистанцию (`r² ≈ 1`).
- **Консоль:** пусто на обеих сборках. **Сборка:** `npm run build` зелёный (`built in 419ms`); JS `452 486 B` против базлайна `452 295 B` — **+191 B**.

### Task 9: Фаза 3 — PR + Merge-gate
**Files:**
- Modify: (сборка/PR)

- [x] коммит + `gh pr create` → **PR #6** (https://github.com/anmaxx/rated/pull/6); способ отката в теле PR. ⚠️ **отклонение (то же, что в Task 6):** база PR — ветка `motion-phase2-hover-modals`, а не `master`: PR #4 и #5 на момент открытия НЕ смержены (мерж — действие владельца). В теле PR — порядок: смержить и прод-сверить #4, затем #5, затем переключить базу #6 на `master` и мержить
- [x] спец-проверки Фазы 3: полный §10 лента (десктоп 1440×900 + мобильная эмуляция 390×844 `mobile,touch` с тач-драгом), замеры скоростей, гард-рейлы (sweep/пустые прямоугольники, пауза-на-ховере, живой RM) — **все сверки дифференциальные против master, поднятого в отдельном `git worktree`**
- [x] пройти **Merge-gate** — автоматизируемая часть пройдена; п.6 (усиленный preview-гейт владельца), п.7 (squash-мерж) и п.8 (прод-сверка) — за владельцем, вынесены чек-листом в тело PR

**Merge-gate Фазы 3 — факт.** Базлайн — `master` (`c90fd59`), собранный в отдельном `git worktree`; сверка дифференциальная: один и тот же скрипт на обеих сборках, вкладка каждый раз выведена на передний план (ловушка троттлинга rAF из Фазы 2).
- **п.1** чистый `npm ci` (снёс `node_modules`) + `npm run build` — зелёные, `built in 419ms`. ⚠️ локально Node 26.5.0, CI собирает на Node 22 — расхождение платформы остаётся непокрытым до рана на master.
- **п.2** `vite preview` (новая — 4173, master — 4174) + chrome-devtools:
  - **автоскролл** новая `33.86 / 34.00 px/s`, master `34.00` (допуск §10 — `34±2`);
  - **пик слот-машины** (модель-независимо: полка фазы `swap` идёт ровно `SPIN_VMAX`, кадр `16.7 мс` снят отдельно) — по 22 прогона на сборку: новая медиана `5000` (разброс `4880–5060`), master медиана `5000` (`4970–5030`);
  - **инерция** — фит по накопленной дистанции `D(t)=v0(k^t−1)/ln k` **только по фазе выбега** (хвост автоскролла обрезан по первому шагу <1.5 px): новая `k = 0.0585 / 0.0595 / 0.0595 / 0.0595`, master `0.0605 ×4` при номинале `0.06`; `r² = 1.0`, RMSE `0.14–0.18 px`; длина выбега на обеих — ровно `53 кадра`;
  - **драг и wheel** — протяжка 10×30 px даёт ровно `300.0 px` на обеих; `deltaX 200` → `+201.1` (новая) и `+200.6` (master);
  - **пауза при наведении** `34 → 0 → 34 px/s` на обеих; **пауза на открытом лайтбоксе** `0`, после Esc `34.01`, `body.overflow` пуст;
  - **клавиатурный `glideIntoView`** — плитка за правым краем (`left 1133`) после `focus()` подведена к `left 28`, целиком в кадре, фокус на ней (master — `1100 → 28`);
  - **«Готика» без двойника + ведение подписи** — четыре перестановки подряд (`Готика`/`хоррор`/`графика`/`биомеханика`): в центре кадра каждый раз оригинал (idx 1–2, `aria-hidden` нет), активная кнопка стиля совпадает; при автоскролле подпись идёт за центром (idx 1 → 2);
  - **гард-рейл sweep** — плиток с висящим `data-src` в кадре и ±200 px: **0**; `firstElementChild` у всех 198 остался `IMG`; 130 из 198 с `src`;
  - **живой RM** (стаб `matchMedia` с рабочей подпиской `change`, отдельные порты 4175/4176): новая `34 → 0 → 33.09 px/s` при 13 подписчиках, master `34 → 0 → 34` при 0 подписчиков (читает `.matches` каждый кадр) — поведение совпадает; под RM клик по стилю на обеих даёт ровно ОДНУ запись `transform`, макс. шаг `0 px`, порядок переставляется сразу;
  - **мобильная эмуляция 390×844 (`mobile,touch`)** — автоскролл `34.00` на обеих, тач-драг (`pointerType: touch`, 8×25 px) ровно `200.0 px` на обеих, `touch-action: pan-y` (вертикальный скролл страницы не заблокирован), `.rt-svc-row` в `48px 1fr 28px`, 0 висящих `data-src`, перестановка ставит в центр оригинал. **Реальный iOS Safari / Android Chrome — за владельцем** (чек-лист в PR).
- ⚠️ **методическая ловушка (к Task 10):** первый заход показал «регресс» — в фазе разгона у новой сборки разрывы между записями `transform` до 40 мс против 21 мс у master, и детектор чистого разгона срабатывал 13 раз из 22 против 22 из 22. **Прямая проверка опровергла:** кадров rAF против записей `transform` в том же окне — новая `315/311` (потеряно 4), master `321/318` (потеряно 3), то есть пропусков кадра нет. Причина видимости — Motion пишет `transform` в другой момент кадра, чем прежний собственный rAF, и таймстемпы MutationObserver дрожат сильнее. Мерить потери кадров только счётом «кадры rAF vs записи», не разрывами MO.
- **п.3** консоль-дифф чист: пусто на обеих сборках на всех прогонах (десктоп, мобильный, RM).
- **п.4** бюджет JS: master `447 630 B` → Фаза 3 `452 486 B`, дельта **+4 856 B raw / +1 848 B gzip** (собственным `gzip -c`; по метрике vite — `447.62 → 452.47 kB`, `135.58 → 137.45 kB` gzip). Из них Фаза 1 дала `+0.06 kB`, Фаза 2 `+4.78 kB` (машинерия `AnimatePresence`), сама Фаза 3 — **`+0.07 kB`**: перевод цикла на `useAnimationFrame` практически бесплатен, Motion уже в бандле.
- **п.5** авто-ревью Codex (`gpt-5.5`, `xhigh`, adversarial) по `motion-phase2-hover-modals...HEAD` — **одна находка, P3, уже известная:** подрезка `delta` до 40 мс на стороне Motion вместо прежних 50 мс (зафиксирована в Task 8 и в комментарии кода). **Измерил масштаб:** при CPU-троттлинге 20× (медиана кадра `33.3 мс`, кадров >40 мс — 15 у новой и 29 у master) автоскролл новая `32.90 px/s`, master `32.87` — разница ниже шума. При 6× просевших кадров нет вовсе. По остальным классам (утечки подписок, гонки с `useLayoutEffect`, гард-рейл `tick`, контракт `firstElementChild`, StrictMode, размонтирование) находок нет.
- **п.6–8** (усиленное подтверждение preview владельцем, squash-мерж, `gh run watch` + прод-сверка) — внешние действия владельца, вынесены чек-листом в тело PR #6.

### Task 10: Финальная сквозная проверка (все фазы вместе)
- [x] сквозной прогон §10 **на `vite preview` прод-сборки ветки со всеми тремя фазами**, НЕ на проде — ⚠️ **прод по-прежнему на master:** отданный ratedtattoo.ru `index.html` ссылается на `index-BG9vuq1J.js`, это побитово хэш локальной сборки `master` (`c90fd59`); PR #4/#5/#6 открыты, мержит владелец. Прод-прогон физически невозможен до мержа — остаётся за владельцем (п.8 Merge-gate каждой фазы)
- [x] reduced-motion чек-лист конечных состояний во всех блоках сразу — прогон одной страницей, все блоки в одной сессии
- [x] (пофазная прод-сверка, мобайл и откат — уже в Merge-gate каждой фазы)

**Сквозной прогон Task 10 (vite preview прод-сборки, `motion-phase3-carousel` = три фазы стеком; порт 4173, базлайн master в отдельном `git worktree` на 4174). Ценность этого прогона против пофазных — все блоки в ОДНОЙ сессии страницы, после реального взаимодействия со всеми остальными.**
- **Reveal:** 10 элементов скрыты при загрузке (`opacity 0 / translateY(46px)`), Hero сразу `opacity 1 / transform none` и `getAnimations() === 0`. Тайминг из WAAPI: `duration 1000`, `easing cubic-bezier(0.22, 1, 0.36, 1)` — точное совпадение. После прохода всех 9 секций скрытых `0`, невидимых блоков `0`, из бегущих анимаций только CSS `rt-pulse` + 3× `rt-eq`. `once`: возврат к `.rt-about-grid` — `0` анимаций, состояние остаётся конечным.
- **Ховеры:** карточка `scale 1 → 1.06 → 1`, дети — две WAAPI-анимации `350 мс / cubic-bezier(0.25,0.1,0.25,1)`; услуга `rgba(236,231,223,0) → 0.03`, номер и стрелка `rgb(95,93,87) → rgb(155,29,48)`, стрелка `translateX(4px)`, уход возвращает ровно значения покоя; DotNav `opacity 0→1`, `translateX 6→0`, `#88857d → #ece7df`, WAAPI `250 мс / EASE_CSS`.
- **Фейд «03»:** `duration 260`, `easing cubic-bezier(0.25, 0.1, 0.25, 1)`, тип объекта — `Animation` (Motion), а не `CSSTransition`; смена клипа отработала (`Тимур за работой` → `It's alive !!!`), конечная `opacity 1`.
- **Модалки:** лайтбокс — вход две панели `240 / EASE_OUT` + overlay `200 / EASE_CSS`, фокус на «Закрыть просмотр», `body.overflow = hidden`, 0 незавершённых через 600 мс; выход — на 40-й и 190-й мс узел ещё в DOM и лок ВСЁ ЕЩЁ стоит, после завершения узла нет, `overflow` пуст, `elementFromPoint` в центре даёт контент страницы. Модалка записи — то же (`240` + `200`). ➕ **Сверено с master:** фокус модалки записи остаётся на `BODY` и на master тоже (автофокуса там не было) — Фаза 2 это поведение не меняла.
- **Лента:** автоскролл `33.99 px/s` (окно 4 с); пик слот-машины по 6 прогонам — медиана `5000`, разброс `4970–5204` (допуск `5000±200`); последовательность разгона `2.2, 4.2, 6.9, 10.6, 14.9, 20, 26.4, 32.8, 40.3, 48.6, 58.1, 68.1` совпадает с базлайном Task 8 покадрово; «Готика» — в центре кадра оригинал (`Вампирша — Готика`, `aria-hidden` нет), активная кнопка стиля подсвечена (`#ece7df` против `#88857d` у остальных); гард-рейл sweep — висящих `data-src` в кадре ±200 px `0`, `firstElementChild` у всех 198 плиток `IMG`; пауза на ховере `33.99 → 0 → 33.99`, пауза на открытом лайтбоксе `0 → 33.99` с пустым `body.overflow` после Esc; драг 10×30 px = ровно `300.0 px`, `deltaX 200` → `+201.7 px`; клавиатурный `glideIntoView` подвёл плитку с `left 2021` к `left 31`, фокус на ней.
- **Reduced-motion — весь чек-лист в одной сессии** (копия прод-сборки со стабом `matchMedia` в `<head>`, отдельный порт, 13 подписчиков): reveal при загрузке `0` скрытых и `0` после полного прохода, Motion-анимаций `0`; лента за 2 с сдвинулась на `0 px`, клик по стилю даёт ровно ОДНУ запись `transform` с макс. шагом `0 px` и ставит в центр оригинал; лайтбокс и модалка записи на ПЕРВОМ кадре уже в конечном состоянии (`opacity 1 / transform none`, `0` WAAPI-анимаций), фокус на «Закрыть просмотр» цел, лок ставится и снимается; закрытие за 60 мс. **Живое переключение:** `34.31 → 0 → 33.99 px/s`. **Флип RM на середине сессии:** 7 ещё не раскрытых блоков ниже экрана после включения RM и доскролла — `0` скрытых, `0` невидимых, `0` анимаций (раскрываются мгновенно, как и должно).
- ⚠️ **Граница метода (не выдавать за проверенное):** CSS-часть RM (`html{scroll-snap-type:none}` и `.rt-rec-dot,.rt-eq>span{animation:none}`) под стабом НЕ срабатывает — стаб подменяет `window.matchMedia`, а не CSS-медиа; в `emulate` chrome-devtools MCP переключателя `prefers-reduced-motion` нет. Проверено СТАТИЧЕСКИ дампом `document.styleSheets`: RM-`@media` в `index.html` содержит ровно эти два правила, мёртвых `.rt-reveal`/`.rt-in`/`.rt-dot:hover` не осталось ни в одной таблице стилей. Живая проверка CSS-RM — на реальном устройстве владельца.
- **Мобильная эмуляция 390×844 (`mobile,touch`), тоже сквозная:** reveal `10 → 0`, невидимых `0`; `.rt-about-grid`/`.rt-faq-grid` в одну колонку (`326px`), `.rt-svc-row` в `48px 1fr 28px`, DotNav `display: none`; автоскролл `34.03 px/s`, тач-драг 8×25 px = ровно `200.0 px`, `touch-action: pan-y`, висящих `data-src` `0`; модалки — вход `240`+`200`, фокус, лок, выход, слоя-перехватчика нет; геометрия картинки в лайтбоксе `81.69/157.63/304.34×430.18` совпадает с базлайном Task 5 (`82/158/304×430`); ховер тач-указателем не включается (`scale` остался `1`) — зафиксированное в Task 3 улучшение.
- **Консоль:** на сборке фаз и на master ровно одно и то же сообщение — `[issue] An element doesn't have an autocomplete attribute (count: 2)` (поля формы записи). Дифф чист.
- ⚠️ **Три ловушки замера (записаны, чтобы не повторять):** (1) React синтезирует `mouseenter`/`mouseleave` из `mouseover`/`mouseout` — «сырой» `mouseenter` он не слышит, из-за чего первый прогон показал ЛОЖНЫЙ «регресс паузы на ховере» (`33.99` вместо `0`); диспатчить `mouseover`/`mouseout` с `relatedTarget`. (2) Среди `document.getAnimations()` без `animationName` есть не только Motion, но и `CSSTransition` — полосу прогресса ролика (`width`, `linear`, 200 мс) легко принять за анимацию Motion; различать по конструктору/`transitionProperty`. (3) Селектор кнопки стиля обязан исключать `.rt-work-tile`: текстовое совпадение по «Готика» попадает в подпись плитки, клик открывает лайтбокс и лента встаёт — прогон даёт `0` записей `transform`.

### Task 11: [Финал] Документация и завершение
- [ ] обновить `docs/animation-motion-migration.md` при отклонениях реализации от дизайна
- [ ] обновить `CLAUDE.md`: новые паттерны (`useReveal`, `usePrefersReducedMotion`) + ⚠️ **снять устаревшую инструкцию `CLAUDE.md:38,46`** («add `className="rt-reveal"`; IntersectionObserver в `App()`») — механизмов нет с Фазы 1, находка P3 авто-ревью; в рабочем дереве лежат чужие незакоммиченные правки этого файла, разобрать с владельцем + **сверить секцию деплоя** (сейчас описывает Babel-в-браузере/`_ds_bundle.js`/`.nojekyll` — устарело под Vite; откат-инструкции должны быть верны)
- [ ] синхронизировать §8.6/§10/§4.4 дизайн-доки с финальными правками (уже частично сделано)
- [ ] обновить Serena-память `mem:session_state_2026_07_20` (через инструменты Serena, не руками)
- [ ] переместить этот план в `docs/plans/completed/`

## Post-Completion
*Требует внешнего действия — без чекбоксов, информационно*

**Мерж и прод:**
- Мерж каждого PR = выкат на прод (ratedtattoo.ru) — действие/подтверждение владельца. Preview-гейт «владелец подтвердил» — на ВСЕ фазы (см. Merge-gate).
- Прод-сверка после КАЖДОГО мержа (ждать зелёного деплой-рана + сверять хэш бандла). Откат — кнопка «Revert» на PR (репо squash-only) → пересборка+деплой (не мгновенно, очередь) → своя прод-сверка.

**Чистка:**
- Удалить смерженные ветки (`motion-phase1-reveal`, `motion-phase2-hover-modals`, `motion-phase3-carousel`), а также ранее смерженные `build-vite`, `works-video-lightbox`.

**Ручная проверка:**
- Мобильные жесты (тач-драг ленты, passive-listeners) на реальных устройствах.
- Перф-наблюдение после Фазы 3 (лента на среднем устройстве) — если просядет, вариант (b)/откат драйвера известен.
